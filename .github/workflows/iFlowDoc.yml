name: 🤖 Generate Documentation for Each iFlow (GitHub Copilot)

on:
  workflow_dispatch:
    inputs:
      package_id:
        description: "Enter the CPI package ID (folder name inside cpi-artifacts)"
        required: true
        type: string

jobs:
  document_iflows:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install jq
        run: |
          sudo apt-get update
          sudo apt-get install -y jq

      - name: Generate Documentation for Each iFlow (Copilot)
        env:
          PKG_ID: ${{ github.event.inputs.package_id }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          API_URL="https://models.inference.ai/github/v1/chat/completions"
          MODEL="gpt-4o-mini"

          PKG_DIR="cpi-artifacts/$PKG_ID"

          # Validate package directory
          if [ ! -d "$PKG_DIR" ]; then
            echo "::error:: Package folder not found: $PKG_ID"
            exit 1
          fi

          echo "📦 Processing package: $PKG_ID"

          SYSTEM_PROMPT="You are a senior SAP CPI Technical Architect.
          Create documentation ONLY for the CPI iFlow based on its artifacts.
          Follow this structure:
          1. High-level architecture
          2. Purpose
          3. Sender/Receiver systems
          4. Adapter types used
          5. End-to-end processing steps
          6. Mapping logic (XML/XSLT/json)
          7. Groovy script logic
          8. Error handling
          9. Security/authentication
          10. Deployment notes"

          # Loop through each iFlow folder
          for FLOW_DIR in "$PKG_DIR"/*; do

            [[ -d "$FLOW_DIR" ]] || continue

            FLOW_NAME=$(basename "$FLOW_DIR")
            OUTPUT_FILE="$FLOW_DIR/IFlow_Summary.md"

            echo "📝 Generating documentation for iFlow: $FLOW_NAME"
            echo "📂 Scanning: $FLOW_DIR"

            # Collect ALL files inside the iFlow folder
            FILES=$(find "$FLOW_DIR" -type f)

            if [ -z "$FILES" ]; then
              echo "⚠ No files found in iFlow: $FLOW_NAME — skipping"
              continue
            fi

            FULL_CONTENT="Artifacts collected for iFlow: $FLOW_NAME\n"

            # Append all file contents
            for FILE in $FILES; do
              CONTENT=$(cat "$FILE")
              FULL_CONTENT="$FULL_CONTENT\n--- START FILE: $FILE ---\n$CONTENT\n--- END FILE: $FILE ---\n"
            done

            USER_QUERY="Generate CPI iFlow documentation using the following artifacts:

$FULL_CONTENT"

            # Build request payload
            PAYLOAD=$(jq -n \
              --arg sys "$SYSTEM_PROMPT" \
              --arg usr "$USER
